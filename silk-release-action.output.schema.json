{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "https://raw.githubusercontent.com/savvy-web/silk-release-action/main/silk-release-action.output.schema.json",
	"anyOf": [
		{
			"$ref": "#/$defs/BranchManagementOutput"
		},
		{
			"$ref": "#/$defs/ValidationOutput"
		},
		{
			"$ref": "#/$defs/PublishingOutput"
		}
	],
	"description": "The phase-discriminated release output contract. Use `phase` to discriminate to the right variant. Four orthogonal state signals (`status`, `noop`, `succeeded`, `hasFailures`) are derived from the same underlying outcome and obey a fixed relationship: `noop` is true when the phase had nothing to do (no changesets, no release-branch updates pending, or no publish targets resolved) — in this case `succeeded` is true and `hasFailures` is false; `status` is `\"no-op\"`. When the phase produced its intended work without errors, `noop` is false, `succeeded` is true, `hasFailures` is false, and `status` is `\"success\"`. When the phase produced any failure, `noop` is false, `succeeded` is false, `hasFailures` is true, and `status` is `\"partial\"`. The `status` value `\"failed\"` is reserved for an impossible flag combination and is never emitted by the current projections; treat `\"partial\"` as the canonical failure label. `status` is a coarse label for logs and summaries; the three booleans are the machine contract. Every variant carries the same shared top-level fields (`$schema`, `schemaVersion`, `phase`, `status`, `noop`, `succeeded`, `hasFailures`, `dryRun`) plus a phase-specific payload.",
	"title": "Silk Release Action output",
	"$defs": {
		"BranchManagementOutput": {
			"type": "object",
			"required": [
				"$schema",
				"schemaVersion",
				"phase",
				"status",
				"noop",
				"succeeded",
				"hasFailures",
				"dryRun",
				"branchManagement"
			],
			"properties": {
				"$schema": {
					"type": "string",
					"enum": [
						"https://raw.githubusercontent.com/savvy-web/silk-release-action/main/silk-release-action.output.schema.json"
					],
					"description": "URL of the hosted JSON Schema this output conforms to. Editors and json-schema-aware consumers use this for hover docs and validation.",
					"title": "JSON Schema URL"
				},
				"schemaVersion": {
					"type": "string",
					"enum": ["1"],
					"description": "In-band schema version. Bumped only on a breaking JSON-shape change (removed/renamed field, changed type) — additive fields do not bump it.",
					"title": "Schema version"
				},
				"phase": {
					"type": "string",
					"enum": ["branch-management"],
					"description": "`branch-management` identifies this as a Phase 1 output.",
					"title": "Phase discriminator"
				},
				"status": {
					"$ref": "#/$defs/ReleaseStatus"
				},
				"noop": {
					"type": "boolean",
					"description": "True when no changesets were found and no release-branch updates were necessary; the phase exits without touching the branch or opening a PR.",
					"title": "No-op"
				},
				"succeeded": {
					"type": "boolean",
					"description": "True when the phase completed cleanly with no failures.",
					"title": "Succeeded"
				},
				"hasFailures": {
					"type": "boolean",
					"description": "True when at least one operation in the phase failed. Set on any failure, including partial ones.",
					"title": "Has failures"
				},
				"dryRun": {
					"type": "boolean",
					"description": "True when the action ran with `dry-run: true`. Phase 3 publishes nothing in dry-run mode; Phases 1 and 2 still observe and report, but mutations (branch updates, PR creation/updating) are suppressed.",
					"title": "Dry run"
				},
				"branchManagement": {
					"$ref": "#/$defs/BranchManagementPayload"
				}
			},
			"additionalProperties": false,
			"description": "The structured `result` output emitted when the action runs in the branch-management phase (Phase 1). Triggered by a push to the target branch; ensures/creates the release branch and the release PR.",
			"title": "Branch Management output (Phase 1)",
			"examples": [
				{
					"$schema": "https://raw.githubusercontent.com/savvy-web/silk-release-action/main/silk-release-action.output.schema.json",
					"schemaVersion": "1",
					"phase": "branch-management",
					"status": "success",
					"noop": false,
					"succeeded": true,
					"hasFailures": false,
					"dryRun": false,
					"branchManagement": {
						"releaseBranch": {
							"name": "changeset-release/main",
							"existed": true,
							"created": false,
							"updated": true,
							"hasConflicts": false
						},
						"releasePr": {
							"number": 42,
							"url": "https://github.com/savvy-web/example-repo/pull/42",
							"action": "updated"
						},
						"changesets": {
							"count": 1,
							"packages": [
								{
									"name": "@savvy-web/example",
									"bumpType": "minor"
								}
							]
						}
					}
				}
			]
		},
		"ReleaseStatus": {
			"type": "string",
			"enum": ["no-op", "success", "partial", "failed"],
			"description": "Coarse human-readable status label, derived from the machine flags. `no-op`: nothing to do this run; `success`: the phase completed cleanly; `partial`: the phase completed but with at least one failure; `failed`: the phase did not succeed and did not produce partial results. Consumers needing failure granularity should read `succeeded`/`hasFailures` and the phase payload instead.",
			"title": "Release status"
		},
		"BranchManagementPayload": {
			"type": "object",
			"required": ["releaseBranch", "releasePr", "changesets"],
			"properties": {
				"releaseBranch": {
					"$ref": "#/$defs/BranchManagementReleaseBranch"
				},
				"releasePr": {
					"anyOf": [
						{
							"$ref": "#/$defs/BranchManagementReleasePr"
						},
						{
							"type": "null"
						}
					]
				},
				"changesets": {
					"$ref": "#/$defs/BranchManagementChangesets"
				}
			},
			"additionalProperties": false,
			"description": "Phase 1 outcome — the release-branch ensure/create result, the release PR (created or updated, null when no changesets), and the changesets observed in `.changeset/` with their derived per-package bumps.",
			"title": "Branch Management payload"
		},
		"BranchManagementReleaseBranch": {
			"type": "object",
			"required": ["name", "existed", "created", "updated", "hasConflicts"],
			"properties": {
				"name": {
					"type": "string",
					"description": "The git branch name the release PR targets and changeset versioning lands on.",
					"title": "Release branch name",
					"examples": ["changeset-release/main"]
				},
				"existed": {
					"type": "boolean",
					"description": "True when the release branch already existed before this run; false when it was just created.",
					"title": "Branch existed"
				},
				"created": {
					"type": "boolean",
					"description": "True when this run created the release branch.",
					"title": "Branch created"
				},
				"updated": {
					"type": "boolean",
					"description": "True when this run pushed new commits to the release branch (changeset version bumps, etc.).",
					"title": "Branch updated"
				},
				"hasConflicts": {
					"type": "boolean",
					"description": "True when the release branch could not be cleanly fast-forwarded over the target branch and a merge conflict was detected. When true, the release branch needs manual conflict resolution before subsequent runs can complete; the action will keep failing on this branch until the conflict is resolved.",
					"title": "Has conflicts"
				}
			},
			"additionalProperties": false,
			"description": "The state of the release branch after Phase 1 — whether it existed, was created/updated, and any conflicts.",
			"title": "Release branch state"
		},
		"BranchManagementReleasePr": {
			"type": "object",
			"required": ["number", "url", "action"],
			"properties": {
				"number": {
					"type": "number",
					"description": "The GitHub PR number for the release PR.",
					"title": "Release PR number"
				},
				"url": {
					"type": "string",
					"description": "The HTML URL of the release PR on GitHub.",
					"title": "Release PR URL",
					"examples": ["https://github.com/owner/repo/pull/123"]
				},
				"action": {
					"type": "string",
					"enum": ["created", "updated"],
					"description": "Whether this run created the release PR or updated an existing one.",
					"title": "Release PR action"
				}
			},
			"additionalProperties": false,
			"description": "The release PR's number, URL, and the action this run took on it. Null when no PR was created (no changesets to release).",
			"title": "Release PR state"
		},
		"BranchManagementChangesets": {
			"type": "object",
			"required": ["count", "packages"],
			"properties": {
				"count": {
					"type": "number",
					"description": "The number of changeset files observed in the `.changeset/` directory.",
					"title": "Changeset count"
				},
				"packages": {
					"type": "array",
					"items": {
						"$ref": "#/$defs/BranchManagementChangesetPackage"
					},
					"description": "The set of packages the observed changesets bump, with the derived semver bump per package. Empty array when no changesets were found; the action emits a no-op in that case.",
					"title": "Changeset packages"
				}
			},
			"additionalProperties": false,
			"description": "Summary of the changesets observed this run — total count and the per-package bump types.",
			"title": "Changesets observed"
		},
		"BranchManagementChangesetPackage": {
			"type": "object",
			"required": ["name", "bumpType"],
			"properties": {
				"name": {
					"type": "string",
					"description": "The npm package name the changeset bumps.",
					"title": "Package name"
				},
				"bumpType": {
					"$ref": "#/$defs/ChangesetsBumpType"
				}
			},
			"additionalProperties": false,
			"description": "One package affected by the observed changesets, with its derived semver bump.",
			"title": "Changeset package"
		},
		"ChangesetsBumpType": {
			"type": "string",
			"enum": ["major", "minor", "patch"],
			"description": "Phase 1's declared bump type, read from changeset frontmatter: `major`, `minor`, or `patch`. The validation phase emits an extended set under `ValidationBumpType` that adds `new` and `unknown`.",
			"title": "Changeset bump type"
		},
		"ValidationOutput": {
			"type": "object",
			"required": [
				"$schema",
				"schemaVersion",
				"phase",
				"status",
				"noop",
				"succeeded",
				"hasFailures",
				"dryRun",
				"validation"
			],
			"properties": {
				"$schema": {
					"type": "string",
					"enum": [
						"https://raw.githubusercontent.com/savvy-web/silk-release-action/main/silk-release-action.output.schema.json"
					],
					"description": "URL of the hosted JSON Schema this output conforms to. Editors and json-schema-aware consumers use this for hover docs and validation.",
					"title": "JSON Schema URL"
				},
				"schemaVersion": {
					"type": "string",
					"enum": ["1"],
					"description": "In-band schema version. Bumped only on a breaking JSON-shape change (removed/renamed field, changed type) — additive fields do not bump it.",
					"title": "Schema version"
				},
				"phase": {
					"type": "string",
					"enum": ["validation"],
					"description": "`validation` identifies this as a Phase 2 output.",
					"title": "Phase discriminator"
				},
				"status": {
					"$ref": "#/$defs/ReleaseStatus"
				},
				"noop": {
					"type": "boolean",
					"description": "True when no packages had version differences against the target branch — either the release has already merged into the target branch, or Phase 1 did not commit the expected version bumps. A run with only version-only packages is NOT a noop; those packages still appear in `publish.packages` with empty `builds`. When this flag is true, a warning-severity finding is also emitted on the `Publish Validation` check so the situation is surfaced to reviewers.",
					"title": "No-op"
				},
				"succeeded": {
					"type": "boolean",
					"description": "True when the phase completed cleanly with no failures.",
					"title": "Succeeded"
				},
				"hasFailures": {
					"type": "boolean",
					"description": "True when at least one operation in the phase failed. Set on any failure, including partial ones.",
					"title": "Has failures"
				},
				"dryRun": {
					"type": "boolean",
					"description": "True when the action ran with `dry-run: true`. Phase 3 publishes nothing in dry-run mode; Phases 1 and 2 still observe and report, but mutations (branch updates, PR creation/updating) are suppressed.",
					"title": "Dry run"
				},
				"validation": {
					"$ref": "#/$defs/ValidationPayload"
				}
			},
			"additionalProperties": false,
			"description": "The structured `result` output emitted when the action runs in the validation phase (Phase 2). Triggered by a push to the release branch; runs build validation, publish dry-runs, release-notes extraction, and emits the unified Release Validation Summary check-run.",
			"title": "Validation output (Phase 2)",
			"examples": [
				{
					"$schema": "https://raw.githubusercontent.com/savvy-web/silk-release-action/main/silk-release-action.output.schema.json",
					"schemaVersion": "1",
					"phase": "validation",
					"status": "success",
					"noop": false,
					"succeeded": true,
					"hasFailures": false,
					"dryRun": false,
					"validation": {
						"buildValidation": {
							"passed": true,
							"packageCount": 1
						},
						"checks": [
							{
								"name": "Build Validation",
								"status": "pass",
								"outcome": "1/1 package(s) built",
								"url": null
							},
							{
								"name": "Link Issues",
								"status": "pass",
								"outcome": "Linked 2 issue(s)",
								"url": "https://github.com/savvy-web/example-repo/runs/123"
							},
							{
								"name": "Publish Validation",
								"status": "pass",
								"outcome": "2/2 target(s) ready",
								"url": "https://github.com/savvy-web/example-repo/runs/123"
							},
							{
								"name": "Release Notes Preview",
								"status": "pass",
								"outcome": "Found release notes for 1 package(s)",
								"url": "https://github.com/savvy-web/example-repo/runs/123"
							},
							{
								"name": "SBOM Preview",
								"status": "pass",
								"outcome": "1/1 SBOM(s) NTIA-compliant",
								"url": "https://github.com/savvy-web/example-repo/runs/123"
							}
						],
						"findings": [],
						"publish": {
							"npmReady": true,
							"githubPackagesReady": true,
							"totalTargets": 2,
							"readyTargets": 2,
							"packages": [
								{
									"name": "@savvy-web/example",
									"version": "1.2.0",
									"baseVersion": "1.1.0",
									"bumpType": "minor",
									"changesetCount": 1,
									"ready": true,
									"versionOnly": false,
									"builds": [
										{
											"directory": "dist/npm",
											"packedBytes": 716,
											"unpackedBytes": 2300,
											"fileCount": 5,
											"sbom": {
												"componentCount": 3,
												"ntiaCompliant": true,
												"missingNtiaFields": []
											},
											"targets": [
												{
													"registry": "https://registry.npmjs.org/",
													"status": "ready",
													"access": "public",
													"provenance": true
												},
												{
													"registry": "https://npm.pkg.github.com/",
													"status": "ready",
													"access": "public",
													"provenance": false
												}
											]
										}
									],
									"releaseNotes": {
										"status": "found",
										"content": "### Minor Changes\n\n- Added the springLaunch API."
									}
								}
							]
						},
						"checkRun": {
							"url": "https://github.com/savvy-web/example-repo/runs/124",
							"conclusion": "success"
						}
					}
				}
			]
		},
		"ValidationPayload": {
			"type": "object",
			"required": ["buildValidation", "checks", "findings", "publish", "checkRun"],
			"properties": {
				"buildValidation": {
					"$ref": "#/$defs/ValidationBuildValidation"
				},
				"checks": {
					"type": "array",
					"items": {
						"$ref": "#/$defs/ValidationCheck"
					},
					"description": "The five-row Validation Checks table — one entry per validation step run this phase. Canonical names: 'Build Validation', 'Link Issues', 'Publish Validation', 'Release Notes Preview', 'SBOM Preview'.",
					"title": "Validation checks"
				},
				"findings": {
					"type": "array",
					"items": {
						"$ref": "#/$defs/ValidationFinding"
					},
					"description": "Every non-pass outcome surfaced by the validation checks, projected for the release PR comment. Empty array when no checks produced an error or warning. Findings preserve the order the checks ran in; the comment renderer reorders errors-before-warnings for display.",
					"title": "Findings"
				},
				"publish": {
					"$ref": "#/$defs/ValidationPublish"
				},
				"checkRun": {
					"anyOf": [
						{
							"$ref": "#/$defs/ValidationCheckRun"
						},
						{
							"type": "null"
						}
					]
				}
			},
			"additionalProperties": false,
			"description": "Phase 2 outcome — the build-validation result, the Validation Checks table, every non-pass finding, the build-centric publish preview (per-package, per-build, per-target readiness), and the unified Release Validation Summary check-run URL.",
			"title": "Validation payload"
		},
		"ValidationBuildValidation": {
			"type": "object",
			"required": ["passed", "packageCount"],
			"properties": {
				"passed": {
					"type": "boolean",
					"description": "True when every released package built successfully.",
					"title": "Build validation passed"
				},
				"packageCount": {
					"type": "number",
					"description": "Number of packages built and validated.",
					"title": "Package count"
				}
			},
			"additionalProperties": false,
			"description": "Summary of the build-validation step: whether every package built, and how many were built.",
			"title": "Build validation summary"
		},
		"ValidationCheck": {
			"type": "object",
			"required": ["name", "status", "outcome", "url"],
			"properties": {
				"name": {
					"type": "string",
					"description": "Row label for the Validation Checks table. Canonical five-row set today: 'Build Validation', 'Link Issues', 'Publish Validation', 'Release Notes Preview', 'SBOM Preview'.",
					"title": "Check name",
					"examples": ["Build Validation", "Link Issues", "Publish Validation", "Release Notes Preview", "SBOM Preview"]
				},
				"status": {
					"type": "string",
					"enum": ["pass", "warning", "error"],
					"description": "`pass` — the check passed; `warning` — the check raised a non-blocking warning; `error` — the check failed and blocks the release.",
					"title": "Check status"
				},
				"outcome": {
					"type": "string",
					"description": "Short human-readable summary of the check's outcome (e.g. counts, registry names, error class).",
					"title": "Outcome message"
				},
				"url": {
					"anyOf": [
						{
							"type": "string",
							"description": "URL pointing to detail for this check (a check-run URL or external report). Null when no URL applies.",
							"title": "Check URL"
						},
						{
							"type": "null"
						}
					]
				}
			},
			"additionalProperties": false,
			"description": "One row of the five-row Validation Checks table shown in the release PR comment and the unified check-run summary.",
			"title": "Validation check row"
		},
		"ValidationFinding": {
			"type": "object",
			"required": ["severity", "check", "scope", "message"],
			"properties": {
				"severity": {
					"type": "string",
					"enum": ["error", "warning"],
					"description": "`error` — fails the validation phase; the release PR is blocked from merging until resolved. `warning` — advisory only and does not block the release; the comment surfaces it for the author to read.",
					"title": "Finding severity"
				},
				"check": {
					"type": "string",
					"description": "The validation check that produced this finding (e.g. `Build Validation`, `Publish Validation`). Equal to the `name` of an entry in `checks[]`. Consumers can group findings under their owning check by joining on this value.",
					"title": "Owning check"
				},
				"scope": {
					"anyOf": [
						{
							"$ref": "#/$defs/ValidationFindingScope"
						},
						{
							"type": "null"
						}
					]
				},
				"message": {
					"type": "string",
					"description": "Human-readable description of the finding, suitable for direct display in the release PR comment.",
					"title": "Finding message"
				}
			},
			"additionalProperties": false,
			"description": "Every non-pass validation outcome — `error` fails the check and blocks the release; `warning` is advisory. Findings are projected for the release PR comment and for downstream consumers of the `result` output.",
			"title": "Validation finding"
		},
		"ValidationFindingScope": {
			"type": "object",
			"required": ["package", "directory"],
			"properties": {
				"package": {
					"anyOf": [
						{
							"type": "string",
							"description": "The npm package name the finding concerns. When non-null, equals the `name` of an entry in `publish.packages[]`. When null, the finding is global to the validation run rather than tied to a specific package.",
							"title": "Package name"
						},
						{
							"type": "null"
						}
					]
				},
				"directory": {
					"anyOf": [
						{
							"type": "string",
							"description": "The build output directory the finding concerns, package-relative. When non-null, equals the `directory` of an entry in the owning package's `builds[]`. When null, the finding concerns the package as a whole rather than a specific build.",
							"title": "Build directory",
							"examples": ["dist/npm", "dist/jsr"]
						},
						{
							"type": "null"
						}
					]
				}
			},
			"additionalProperties": false,
			"description": "What a non-pass finding concerns: which package and which of its build directories. `package` is null for global findings; `directory` is null for findings not tied to a specific build directory.",
			"title": "Validation finding scope"
		},
		"ValidationPublish": {
			"type": "object",
			"required": ["npmReady", "githubPackagesReady", "totalTargets", "readyTargets", "packages"],
			"properties": {
				"npmReady": {
					"type": "boolean",
					"description": "True when every npm publish target passed its dry-run probe.",
					"title": "npm ready"
				},
				"githubPackagesReady": {
					"type": "boolean",
					"description": "True when every GitHub Packages publish target passed its dry-run probe.",
					"title": "GitHub Packages ready"
				},
				"totalTargets": {
					"type": "number",
					"description": "Total number of publish targets across every released package and every registry.",
					"title": "Total targets"
				},
				"readyTargets": {
					"type": "number",
					"description": "Number of publish targets that passed their dry-run probe.",
					"title": "Ready targets"
				},
				"packages": {
					"type": "array",
					"items": {
						"$ref": "#/$defs/ValidationPublishPackage"
					},
					"description": "The packages being released this run, with their builds and per-target readiness. Empty array only when no packages had version differences against the target branch — in that case the run is a noop and a warning-severity finding is emitted to explain why. A release that bumps only private/version-only packages still populates this array, with empty `builds` per package.",
					"title": "Released packages"
				}
			},
			"additionalProperties": false,
			"description": "Build-centric publish preview — per-registry readiness rollup plus the full per-package, per-build, per-target breakdown.",
			"title": "Publish preview"
		},
		"ValidationPublishPackage": {
			"type": "object",
			"required": [
				"name",
				"version",
				"baseVersion",
				"bumpType",
				"changesetCount",
				"ready",
				"versionOnly",
				"builds",
				"releaseNotes"
			],
			"properties": {
				"name": {
					"type": "string",
					"description": "The npm package name being released.",
					"title": "Package name"
				},
				"version": {
					"type": "string",
					"description": "The new semver version this release would publish.",
					"title": "New version",
					"examples": ["1.2.3", "0.4.0-beta.1"]
				},
				"baseVersion": {
					"anyOf": [
						{
							"type": "string",
							"description": "The previously published version this release is bumped from. Null when this is the first publish.",
							"title": "Base version"
						},
						{
							"type": "null"
						}
					]
				},
				"bumpType": {
					"$ref": "#/$defs/ValidationBumpType"
				},
				"changesetCount": {
					"anyOf": [
						{
							"type": "number",
							"description": "Number of changesets contributing to this package's bump. Null when unknown.",
							"title": "Changeset count"
						},
						{
							"type": "null"
						}
					]
				},
				"ready": {
					"type": "boolean",
					"description": "True when every publish target for this package's builds passed its dry-run probe.",
					"title": "Ready"
				},
				"versionOnly": {
					"type": "boolean",
					"description": "True when the package has no publish targets — only a GitHub release (and tag) is produced. Used for repos that version a package but don't publish it to a registry.",
					"title": "Version-only"
				},
				"builds": {
					"type": "array",
					"items": {
						"$ref": "#/$defs/ValidationBuild"
					},
					"description": "The unique output directories this package produces, one entry per build.",
					"title": "Builds"
				},
				"releaseNotes": {
					"$ref": "#/$defs/ValidationReleaseNotes"
				}
			},
			"additionalProperties": false,
			"description": "A package being released this run, with its bump type, builds, per-target readiness, and the extracted release notes.",
			"title": "Released package"
		},
		"ValidationBumpType": {
			"type": "string",
			"enum": ["major", "minor", "patch", "new", "unknown"],
			"description": "The validation phase's package bump type, derived by diffing the release-branch version against the target-branch version. A superset of `ChangesetsBumpType` (Phase 1 declared bumps): `major`/`minor`/`patch` are the standard semver bumps, and this enum adds `new` (no prior published version exists on the target branch) and `unknown` (could not be determined, typically when the prior version was a pre-release tag).",
			"title": "Bump type"
		},
		"ValidationBuild": {
			"type": "object",
			"required": ["directory", "packedBytes", "unpackedBytes", "fileCount", "sbom", "targets"],
			"properties": {
				"directory": {
					"type": "string",
					"description": "Package-relative path to the build's output directory. One build is produced per unique output directory; the tarball is packed once and shared across all targets publishing this directory.",
					"title": "Build directory",
					"examples": ["dist/npm", "dist/jsr"]
				},
				"packedBytes": {
					"anyOf": [
						{
							"type": "number",
							"description": "Size of the packed tarball in bytes. Null when the dry-run did not report it.",
							"title": "Packed size (bytes)"
						},
						{
							"type": "null"
						}
					]
				},
				"unpackedBytes": {
					"anyOf": [
						{
							"type": "number",
							"description": "Size of the unpacked contents in bytes. Null when the dry-run did not report it.",
							"title": "Unpacked size (bytes)"
						},
						{
							"type": "null"
						}
					]
				},
				"fileCount": {
					"anyOf": [
						{
							"type": "number",
							"description": "Number of files in the packed tarball. Null when the dry-run did not report it.",
							"title": "File count"
						},
						{
							"type": "null"
						}
					]
				},
				"sbom": {
					"anyOf": [
						{
							"$ref": "#/$defs/ValidationBuildSbom"
						},
						{
							"type": "null"
						}
					]
				},
				"targets": {
					"type": "array",
					"items": {
						"$ref": "#/$defs/ValidationBuildTarget"
					},
					"description": "The registry targets this build would publish to, with per-target readiness.",
					"title": "Publish targets"
				}
			},
			"additionalProperties": false,
			"description": "One unique output directory of a released package. The tarball is packed once and shared across all registry targets publishing this directory; per-target readiness is enumerated in `targets`.",
			"title": "Build"
		},
		"ValidationBuildSbom": {
			"type": "object",
			"required": ["componentCount", "ntiaCompliant", "missingNtiaFields"],
			"properties": {
				"componentCount": {
					"type": "number",
					"description": "Number of components (direct + transitive dependencies) in the BOM. 0 is legitimate for a dependency-free package.",
					"title": "Component count"
				},
				"ntiaCompliant": {
					"type": "boolean",
					"description": "True when the BOM satisfies every NTIA minimum-elements field. False when at least one field is missing — see `missingNtiaFields`.",
					"title": "NTIA compliant"
				},
				"missingNtiaFields": {
					"type": "array",
					"items": {
						"type": "string"
					},
					"description": "Names of the NTIA minimum-elements fields the BOM is missing. Empty when `ntiaCompliant` is true.",
					"title": "Missing NTIA fields",
					"examples": [["Supplier Name", "Author"], ["Timestamp"]]
				}
			},
			"additionalProperties": false,
			"description": "Per-build SBOM preview: component count, NTIA minimum-elements compliance, and the list of missing NTIA fields when not compliant.",
			"title": "SBOM preview"
		},
		"ValidationBuildTarget": {
			"type": "object",
			"required": ["registry", "status", "access", "provenance"],
			"properties": {
				"registry": {
					"type": "string",
					"description": "The registry endpoint this target would publish to.",
					"title": "Registry URL",
					"examples": ["https://registry.npmjs.org/", "https://npm.pkg.github.com/", "https://jsr.io"]
				},
				"status": {
					"type": "string",
					"enum": ["ready", "skipped", "failed"],
					"description": "`ready` — the dry-run publish probe succeeded and the target is ready to publish; `skipped` — the target was intentionally not probed (e.g. unconfigured or filtered out); `failed` — the dry-run probe failed and the target would not publish.",
					"title": "Target readiness"
				},
				"access": {
					"type": "string",
					"enum": ["public", "restricted"],
					"description": "`public` — the package would publish publicly; `restricted` — the package would publish privately (scoped, restricted access).",
					"title": "Access level"
				},
				"provenance": {
					"type": "boolean",
					"description": "True when the target supports and would emit npm OIDC sigstore provenance attestations.",
					"title": "Provenance"
				}
			},
			"additionalProperties": false,
			"description": "Per-registry publish readiness for a single build directory: `ready` / `skipped` / `failed`, plus access level and provenance support.",
			"title": "Publish target"
		},
		"ValidationReleaseNotes": {
			"anyOf": [
				{
					"$ref": "#/$defs/ReleaseNotesFound"
				},
				{
					"$ref": "#/$defs/ReleaseNotesNoChangelog"
				},
				{
					"$ref": "#/$defs/ReleaseNotesVersionNotFound"
				},
				{
					"$ref": "#/$defs/ReleaseNotesError"
				}
			],
			"description": "Discriminated outcome of reading the package's CHANGELOG.md (already populated by `changeset version`) and locating the section for the new version: `found`, `no-changelog`, `version-not-found`, or `error`.",
			"title": "Release notes extraction"
		},
		"ReleaseNotesFound": {
			"type": "object",
			"required": ["status", "content"],
			"properties": {
				"status": {
					"type": "string",
					"enum": ["found"],
					"description": "The CHANGELOG.md was found and the section for the new version was successfully extracted.",
					"title": "Found"
				},
				"content": {
					"type": "string",
					"description": "The extracted Markdown content of the CHANGELOG.md section for the new version.",
					"title": "Release notes content"
				}
			},
			"additionalProperties": false,
			"description": "The CHANGELOG.md exists and the section for the new version was located and extracted.",
			"title": "Release notes found"
		},
		"ReleaseNotesNoChangelog": {
			"type": "object",
			"required": ["status"],
			"properties": {
				"status": {
					"type": "string",
					"enum": ["no-changelog"],
					"description": "The package has no CHANGELOG.md file — no release notes can be extracted.",
					"title": "No CHANGELOG.md"
				}
			},
			"additionalProperties": false,
			"description": "The package has no CHANGELOG.md file. This is non-fatal; the GitHub release is created with the version-bump summary instead.",
			"title": "Release notes — no CHANGELOG.md"
		},
		"ReleaseNotesVersionNotFound": {
			"type": "object",
			"required": ["status", "reason"],
			"properties": {
				"status": {
					"type": "string",
					"enum": ["version-not-found"],
					"description": "The CHANGELOG.md exists but no section for the new version was found.",
					"title": "Version not found"
				},
				"reason": {
					"type": "string",
					"description": "Human-readable explanation of why the version section was not located (e.g. parser couldn't find a matching heading).",
					"title": "Reason"
				}
			},
			"additionalProperties": false,
			"description": "The CHANGELOG.md exists but no section for the new version was found.",
			"title": "Release notes — version not found"
		},
		"ReleaseNotesError": {
			"type": "object",
			"required": ["status", "message"],
			"properties": {
				"status": {
					"type": "string",
					"enum": ["error"],
					"description": "An error occurred while attempting to read or parse the CHANGELOG.md.",
					"title": "Error"
				},
				"message": {
					"type": "string",
					"description": "The error message from the failed read/parse operation.",
					"title": "Error message"
				}
			},
			"additionalProperties": false,
			"description": "An error occurred while attempting to read or parse the CHANGELOG.md. Non-fatal — the release still proceeds; the GitHub Release body falls back to an auto-generated bump summary.",
			"title": "Release notes — error"
		},
		"ValidationCheckRun": {
			"type": "object",
			"required": ["url", "conclusion"],
			"properties": {
				"url": {
					"type": "string",
					"description": "The HTML URL of the unified Release Validation Summary check-run.",
					"title": "Check-run URL",
					"examples": ["https://github.com/owner/repo/runs/123456789"]
				},
				"conclusion": {
					"type": "string",
					"enum": ["success", "failure", "neutral", "cancelled", "skipped", "timed_out", "action_required"],
					"description": "The GitHub check-run conclusion enum: `success`, `failure`, `neutral`, `cancelled`, `skipped`, `timed_out`, or `action_required`.",
					"title": "Check-run conclusion"
				}
			},
			"additionalProperties": false,
			"description": "The unified Release Validation Summary check-run produced by the validation phase. Null when no check-run was created.",
			"title": "Validation check-run"
		},
		"PublishingOutput": {
			"type": "object",
			"required": [
				"$schema",
				"schemaVersion",
				"phase",
				"status",
				"noop",
				"succeeded",
				"hasFailures",
				"dryRun",
				"publishing"
			],
			"properties": {
				"$schema": {
					"type": "string",
					"enum": [
						"https://raw.githubusercontent.com/savvy-web/silk-release-action/main/silk-release-action.output.schema.json"
					],
					"description": "URL of the hosted JSON Schema this output conforms to. Editors and json-schema-aware consumers use this for hover docs and validation.",
					"title": "JSON Schema URL"
				},
				"schemaVersion": {
					"type": "string",
					"enum": ["1"],
					"description": "In-band schema version. Bumped only on a breaking JSON-shape change (removed/renamed field, changed type) — additive fields do not bump it.",
					"title": "Schema version"
				},
				"phase": {
					"type": "string",
					"enum": ["publishing"],
					"description": "`publishing` identifies this as a Phase 3 output.",
					"title": "Phase discriminator"
				},
				"status": {
					"$ref": "#/$defs/ReleaseStatus"
				},
				"noop": {
					"type": "boolean",
					"description": "True when there were no publish targets resolved — every released package was version-only or was already published at the same digest; nothing was sent to any registry.",
					"title": "No-op"
				},
				"succeeded": {
					"type": "boolean",
					"description": "True when the phase completed cleanly with no failures.",
					"title": "Succeeded"
				},
				"hasFailures": {
					"type": "boolean",
					"description": "True when at least one operation in the phase failed. Set on any failure, including partial ones.",
					"title": "Has failures"
				},
				"dryRun": {
					"type": "boolean",
					"description": "True when the action ran with `dry-run: true`. Phase 3 publishes nothing in dry-run mode; Phases 1 and 2 still observe and report, but mutations (branch updates, PR creation/updating) are suppressed.",
					"title": "Dry run"
				},
				"publishing": {
					"$ref": "#/$defs/PublishingPayload"
				}
			},
			"additionalProperties": false,
			"description": "The structured `result` output emitted when the action runs in the publishing phase (Phase 3). Triggered by the merge of the release PR; publishes to every configured registry, generates SBOM/provenance attestations, and creates GitHub releases and tags.",
			"title": "Publishing output (Phase 3)",
			"examples": [
				{
					"$schema": "https://raw.githubusercontent.com/savvy-web/silk-release-action/main/silk-release-action.output.schema.json",
					"schemaVersion": "1",
					"phase": "publishing",
					"status": "success",
					"noop": false,
					"succeeded": true,
					"hasFailures": false,
					"dryRun": false,
					"publishing": {
						"packages": [
							{
								"name": "@savvy-web/example",
								"version": "1.2.0",
								"status": "published",
								"skipReason": null,
								"targets": [
									{
										"registry": "https://registry.npmjs.org/",
										"status": "published",
										"skipReason": null,
										"recovery": null,
										"registryUrl": "https://www.npmjs.com/package/@savvy-web/example/v/1.2.0",
										"error": null,
										"attestationRecovered": false,
										"sbomAttestationRecovered": false
									},
									{
										"registry": "https://npm.pkg.github.com/",
										"status": "published",
										"skipReason": null,
										"recovery": null,
										"registryUrl": "https://github.com/savvy-web/example-repo/packages/12345",
										"error": null,
										"attestationRecovered": false,
										"sbomAttestationRecovered": false
									}
								],
								"attestations": {
									"provenanceUrl": "https://search.sigstore.dev/?logIndex=12345",
									"sbomUrl": "https://github.com/savvy-web/example-repo/attestations/123",
									"githubAttestationUrl": "https://github.com/savvy-web/example-repo/attestations/124"
								},
								"tarballDigest": "sha512-Vb1g8tXp4l8a9bC..."
							}
						],
						"tags": [
							{
								"name": "@savvy-web/example@1.2.0",
								"sha": "abc123def456abc123def456abc123def456abc1",
								"packageName": "@savvy-web/example"
							}
						],
						"releases": [
							{
								"tag": "@savvy-web/example@1.2.0",
								"url": "https://github.com/savvy-web/example-repo/releases/tag/@savvy-web/example@1.2.0",
								"id": 12345678,
								"packageName": "@savvy-web/example"
							}
						]
					}
				}
			]
		},
		"PublishingPayload": {
			"type": "object",
			"required": ["packages", "tags", "releases"],
			"properties": {
				"packages": {
					"type": "array",
					"items": {
						"$ref": "#/$defs/PublishPackage"
					},
					"description": "The packages processed by the publishing phase, with their per-target outcomes. Empty array when there were no packages to publish (the action emits a no-op). In a dry-run, the array is populated with simulated results — each package's `status` is the outcome the action would produce on a real run.",
					"title": "Published packages"
				},
				"tags": {
					"type": "array",
					"items": {
						"$ref": "#/$defs/PublishingTag"
					},
					"description": "Git tags created by the publishing phase (one per released package or one for the whole release, depending on workflow). Empty array when no git tags were created — either the run was a no-op, or it failed before reaching the tagging step. A populated array means tags were created on the release commit.",
					"title": "Release tags"
				},
				"releases": {
					"type": "array",
					"items": {
						"$ref": "#/$defs/PublishingRelease"
					},
					"description": "GitHub releases created by the publishing phase. Empty array when no GitHub Releases were created — same conditions as `tags`. The release entries pair 1:1 with tags by `tag` name (and `packageName`).",
					"title": "GitHub releases"
				}
			},
			"additionalProperties": false,
			"description": "Phase 3 outcome — the per-package publish results (with attestation URLs and tarball digests), the git tags created, and the GitHub releases created.",
			"title": "Publishing payload"
		},
		"PublishPackage": {
			"type": "object",
			"required": ["name", "version", "status", "skipReason", "targets", "attestations", "tarballDigest"],
			"properties": {
				"name": {
					"type": "string",
					"description": "The npm package name that was (or would have been) published.",
					"title": "Package name"
				},
				"version": {
					"type": "string",
					"description": "The semver version this run published.",
					"title": "Published version"
				},
				"status": {
					"type": "string",
					"enum": ["published", "skipped", "failed"],
					"description": "`published` — at least one target accepted the publish; `skipped` — every target was skipped (no work landed); `failed` — every target either failed or skipped, but at least one failed.",
					"title": "Package publish status"
				},
				"skipReason": {
					"anyOf": [
						{
							"$ref": "#/$defs/PublishPackageSkipReason"
						},
						{
							"type": "null"
						}
					]
				},
				"targets": {
					"type": "array",
					"items": {
						"$ref": "#/$defs/PublishTarget"
					},
					"description": "The per-registry publish outcomes for this package.",
					"title": "Publish targets"
				},
				"attestations": {
					"$ref": "#/$defs/PublishPackageAttestations"
				},
				"tarballDigest": {
					"anyOf": [
						{
							"type": "string",
							"description": "Integrity hash of the published tarball, expressed as Subresource-Integrity-style `sha512-<base64>`. `null` for skipped or failed publishes.",
							"title": "Tarball digest",
							"examples": ["sha512-Vb1g8tXp4l8a9bC..."]
						},
						{
							"type": "null"
						}
					]
				}
			},
			"additionalProperties": false,
			"description": "A package that was processed by the publishing phase, with its per-target outcomes, attestation URLs, and the integrity digest of the published tarball.",
			"title": "Published package"
		},
		"PublishPackageSkipReason": {
			"type": "string",
			"enum": ["already-published-identical", "already-published-unknown"],
			"description": "`already-published-identical` — the version is already published and the tarball digest matches what would be published; `already-published-unknown` — the version is already published but the on-registry tarball digest could not be confirmed (advisory only — verify by hand if tarball-digest parity matters; the publish was skipped because the registry has the version but its identity could not be confirmed). Null when the package was not skipped.",
			"title": "Skip reason"
		},
		"PublishTarget": {
			"type": "object",
			"required": [
				"registry",
				"status",
				"skipReason",
				"recovery",
				"registryUrl",
				"error",
				"attestationRecovered",
				"sbomAttestationRecovered"
			],
			"properties": {
				"registry": {
					"type": "string",
					"description": "The registry endpoint this target published to.",
					"title": "Registry URL",
					"examples": ["https://registry.npmjs.org/", "https://npm.pkg.github.com/", "https://jsr.io"]
				},
				"status": {
					"type": "string",
					"enum": ["published", "skipped", "failed"],
					"description": "`published` — the package was successfully published to this target; `skipped` — the publish was intentionally not attempted (e.g. dry-run, no token, already-published); `failed` — the publish call returned an error and the package was not published to this target. A `failed` target means the underlying error is in the per-target `error` field, and the run's overall `hasFailures` flag is set.",
					"title": "Publish status"
				},
				"skipReason": {
					"anyOf": [
						{
							"$ref": "#/$defs/PublishTargetSkipReason"
						},
						{
							"type": "null"
						}
					]
				},
				"recovery": {
					"anyOf": [
						{
							"$ref": "#/$defs/PublishTargetRecovery"
						},
						{
							"type": "null"
						}
					],
					"description": "Digest pair recorded when the orchestrator made a recovery decision against this target's registry — both the recovery-skip (`skipReason: already-published-identical`) and the fatal integrity-mismatch (`status: failed`) outcomes carry it. Null when the publish flowed straight through to upload.",
					"title": "Recovery digests"
				},
				"registryUrl": {
					"anyOf": [
						{
							"type": "string",
							"description": "URL of the published artifact on the registry. Null when the publish did not produce a discoverable URL.",
							"title": "Published artifact URL"
						},
						{
							"type": "null"
						}
					]
				},
				"error": {
					"anyOf": [
						{
							"type": "string",
							"description": "Error message when `status` is `failed`. Null on `published` or `skipped`.",
							"title": "Error message"
						},
						{
							"type": "null"
						}
					]
				},
				"attestationRecovered": {
					"anyOf": [
						{
							"type": "boolean"
						},
						{
							"type": "null"
						}
					],
					"description": "True when the provenance attestation already existed for this tarball's sha256 and the orchestrator reused the existing URL instead of writing a new one. False when a new attestation was written this run. Null when no attestation step was attempted (provenance: false on every target in the group, or the target itself was not in a successful state).",
					"title": "Provenance attestation recovered"
				},
				"sbomAttestationRecovered": {
					"anyOf": [
						{
							"type": "boolean"
						},
						{
							"type": "null"
						}
					],
					"description": "True when the SBOM attestation already existed for this tarball's sha256 and the orchestrator reused the existing URL instead of writing a new one. False when a new attestation was written this run. Null when no SBOM attestation was attempted.",
					"title": "SBOM attestation recovered"
				}
			},
			"additionalProperties": false,
			"description": "Per-registry publish outcome — what was attempted, whether it succeeded, and the URL of the published artifact.",
			"title": "Published target"
		},
		"PublishTargetSkipReason": {
			"type": "string",
			"enum": ["already-published-identical"],
			"description": "`already-published-identical` — the version was already on this specific registry and the registry's stored integrity matched the locally-packed digest, so the orchestrator recovered the target rather than re-uploading. Null when the target was not skipped. Finer-grained than the package-level `skipReason`: this fires per target, so a mixed result (one target published, one recovered) records the recovery on the target itself.",
			"title": "Target skip reason"
		},
		"PublishTargetRecovery": {
			"type": "object",
			"required": ["localDigest", "remoteDigest"],
			"properties": {
				"localDigest": {
					"type": "string",
					"description": "Integrity digest of the locally-packed tarball, in npm's `dist.integrity` format (`sha512-<base64>`). Equals what the orchestrator would have uploaded for this target.",
					"title": "Local digest"
				},
				"remoteDigest": {
					"type": "string",
					"description": "Integrity digest the target registry already has on file for this package version, in npm's `dist.integrity` format (`sha512-<base64>`). Equals `localDigest` on the `skipped` (`already-published-identical`) branch; differs on the `failed` integrity-mismatch branch.",
					"title": "Remote digest"
				}
			},
			"additionalProperties": false,
			"description": "Pair of digests recorded when the orchestrator probed the target's registry and made a recovery decision. Present on `skipped` (`skipReason: already-published-identical`) and on `failed` integrity-mismatch outcomes; null when the publish flowed straight through to upload.",
			"title": "Recovery digest pair"
		},
		"PublishPackageAttestations": {
			"type": "object",
			"required": ["provenanceUrl", "sbomUrl", "githubAttestationUrl"],
			"properties": {
				"provenanceUrl": {
					"anyOf": [
						{
							"type": "string",
							"description": "URL of the npm OIDC sigstore provenance attestation. Null when no provenance was emitted.",
							"title": "Provenance attestation URL"
						},
						{
							"type": "null"
						}
					]
				},
				"sbomUrl": {
					"anyOf": [
						{
							"type": "string",
							"description": "URL of the CycloneDX SBOM attestation uploaded to the artifact store. Null when no SBOM attestation was emitted.",
							"title": "SBOM attestation URL"
						},
						{
							"type": "null"
						}
					]
				},
				"githubAttestationUrl": {
					"anyOf": [
						{
							"type": "string",
							"description": "URL of the GitHub artifact-attestation (`gh attestation verify`). Null when no GitHub attestation was emitted.",
							"title": "GitHub attestation URL"
						},
						{
							"type": "null"
						}
					]
				}
			},
			"additionalProperties": false,
			"description": "Per-package attestation URLs emitted during publishing — provenance, SBOM, and the GitHub attestation.",
			"title": "Attestations"
		},
		"PublishingTag": {
			"type": "object",
			"required": ["name", "sha", "packageName"],
			"properties": {
				"name": {
					"type": "string",
					"description": "The git tag name created for the release.",
					"title": "Tag name",
					"examples": ["v1.2.3", "@savvy-web/example@1.2.3"]
				},
				"sha": {
					"type": "string",
					"description": "The commit SHA the tag points at.",
					"title": "Tag SHA"
				},
				"packageName": {
					"anyOf": [
						{
							"type": "string",
							"description": "The package name this tag belongs to. Non-null for per-package tags in multi-package release mode (the tag name itself is the npm-style `@scope/pkg@version`). Null for an aggregated tag covering every released package (the `vSEMVER` shape in fixed-release mode).",
							"title": "Package name"
						},
						{
							"type": "null"
						}
					]
				}
			},
			"additionalProperties": false,
			"description": "A git tag created for the release.",
			"title": "Release tag"
		},
		"PublishingRelease": {
			"type": "object",
			"required": ["tag", "url", "id", "packageName"],
			"properties": {
				"tag": {
					"type": "string",
					"description": "The git tag the GitHub release is attached to.",
					"title": "Tag"
				},
				"url": {
					"type": "string",
					"description": "The HTML URL of the GitHub release.",
					"title": "Release URL"
				},
				"id": {
					"type": "number",
					"description": "The numeric GitHub release ID.",
					"title": "Release ID"
				},
				"packageName": {
					"anyOf": [
						{
							"type": "string",
							"description": "The package name this release belongs to. Non-null for per-package releases in multi-package release mode (the release pairs with the tag of the same `tag` value). Null for an aggregated release covering every released package (the `vSEMVER` shape in fixed-release mode).",
							"title": "Package name"
						},
						{
							"type": "null"
						}
					]
				}
			},
			"additionalProperties": false,
			"description": "A GitHub release created by the publishing phase.",
			"title": "GitHub release"
		}
	}
}
